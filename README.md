Name your next project with a common English word.

1. Change the word in `abbv`.
2. Customize your preferences (word length filter, etc)
3. Then run

```sh
rustc abbreviation.rs && ./abbreviation
```
